# first-class-agent-hosts

Rebuild c3spec around first-class Cursor, Claude Code, and Codex host support with canonical .agents artifacts, host renderers, subagents, hooks, and removal of unsupported adapters.
