# Tasks: pi-only-package-pivot

- [ ] 1. Update workflow/spec contracts to pi-only across affected delta specs.
- [ ] 2. Refactor routing/instruction surfaces to remove non-pi host language.
- [ ] 3. Remove core host-generation enums/renderers/adapters for Cursor/Claude/Codex.
- [ ] 4. Remove or rewrite host-specific tests; add pi-only contract coverage.
- [ ] 5. Add pi package manifest/resource wiring in `package.json` and project layout updates.
- [ ] 6. Implement pi-native capability uplift slice(s) using extension/SDK/runtime primitives.
- [ ] 7. Update default docs and onboarding paths to pi-only guidance.
- [ ] 8. Run full verification (build/tests/contract checks/grep drift sweep).
- [ ] 9. Write retrospective and capture memory if learning generalizes.
- [ ] 10. Sync delta specs to main specs (or explicitly record no-sync decision) before archive.
