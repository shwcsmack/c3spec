# Tasks — remove-schema-system

- [x] Remove schema-focused CLI entry points (`schema`, `schemas`, `templates`)
- [x] Remove schema flags from primary workflow commands (`status`, `instructions`, `new change`)
- [x] Update workflow tests to reflect schema-less CLI surface
- [x] Remove schema resolution/template-loading internals from artifact-graph runtime
- [x] Remove schema metadata dependence from change creation/status paths
- [x] Delete schema directories (`schemas/*`, `c3spec/schemas/superpowers-bridge`)
- [x] Update docs/specs to final non-schema architecture
- [ ] Verify and archive