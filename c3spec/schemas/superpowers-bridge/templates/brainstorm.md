<!--
Tier 3 brainstorm artifact — generated by superpowers:brainstorming.

HTML REVIEW CONVENTION (required for Tier 3):
1. Generate an HTML version of this artifact first at the same path with .html extension
2. Print immediately: "Brainstorm HTML ready — paste into browser: file:///[absolute path].html"
3. Wait for user to review and approve (or request changes)
4. Save this markdown file only after approval

The HTML version should include: decision tree visualization (SVG or nested lists),
color-coded alternatives (considered vs. chosen), code snippets with syntax highlighting.
The HTML is kept alongside this .md for reference; it is NOT committed (add *.html to
a local .gitignore if desired, or commit both — user's preference).

Raw capture of superpowers:brainstorming output.
Skill output is typically a decision log (context → decision chain Q1-Qn → design trade-offs)
but may vary by conversation shape.

design.md extracts and reorganizes this into structured decisions.
Do NOT copy content from here into design.md — design.md is an independent reorganization.
-->

## HTML Review

> Before saving this file, generate the HTML version and present the file path.
> Delete this section from the saved markdown after approval.
